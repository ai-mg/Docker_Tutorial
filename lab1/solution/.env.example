# Copy to .env and adjust if needed. docker compose picks .env up automatically.
POSTGRES_USER=iris
POSTGRES_PASSWORD=iris_pw
POSTGRES_DB=iris
DATABASE_URL=postgresql+psycopg2://iris:iris_pw@db:5432/iris
